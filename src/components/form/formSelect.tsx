import { Select } from "@mui/material";
/**
 * @todo implement the select component to allow the user to choose the type of client
 * Just to know what is my intention and what I want to do.
 * @todo Should also creating a custom component for a button.
 */
export const FormSelect = () => {
  return <Select />;
};
